[pytest]
addopts = -ra
testpaths = tests
pythonpath = src
markers =
    unit: Fast unit tests.
    integration: Multi-process integration tests.
    slow: Long-running tests that spawn many worker processes; may fail under memory pressure when the full test suite is run concurrently.