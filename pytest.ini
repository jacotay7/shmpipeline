[pytest]
addopts = -ra
testpaths = tests
pythonpath = src
markers =
    unit: Fast unit tests.
    integration: Multi-process integration tests.
    slow: Long-running tests that spawn many worker processes; run these in a separate pytest invocation (CI does this) so accumulated numba/CUDA JIT state cannot exhaust memory mid-session.